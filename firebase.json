
{
  "functions": {
    "source": "functions",
    "predeploy": [
      "npm run build && rm -rf functions/nuxt && cp -r .nuxt/ functions/nuxt/ && cp nuxt.config.js functions/"
    ]
  },
  "hosting": {
    "predeploy": [
      "rm -rf dist/* && mkdir -p dist/_nuxt && cp -r .nuxt/dist/client/ dist/_nuxt && cp -a static/. dist/"
    ],
    "public": "public",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "function": "ssrapp"
      }
    ]
  }
}
